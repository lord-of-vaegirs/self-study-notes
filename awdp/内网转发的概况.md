## 内网转发的概况

### 内网转发的三种形式
1. 端口转发
2. 端口映射（反弹shell）
3. 代理转发

### 四种基本的网络情况
1. 攻击者有独立的外网IP，拿到shell的服务器也有独立的外网IP
2. 攻击者有独立的外网IP，拿到shell的服务器在内网，只有某一个或者几个映射端口
3. 攻击者在内网，通过内网的公共路由器出去，拿到shell的服务器在内网，只有某一个或者几个映射端口
4. 攻击者在内网，通过内网的公共路由器出去，拿到shell的服务器也有独立的外网IP